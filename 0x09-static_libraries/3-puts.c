#include "main.h"
#include <stdio.h>

/**
 * _puts - prints the string folowed by a new line
 * @str: Pointer to the input
 * Return: Always 0 (Success)
 * Author: IanoNjuguna
 */
void _puts(char *str)
{
	printf("%s\n", str);
}

