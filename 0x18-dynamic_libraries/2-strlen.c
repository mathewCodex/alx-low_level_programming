#include "main.h"

/**
 * _strlen - returns the len of a str
 *
 * @s: hold each xtics after the count
 * Return: len of a str
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}
